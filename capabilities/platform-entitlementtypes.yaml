naftiko: 1.0.0-alpha2
info:
  label: AppyWay Platform API — EntitlementTypes
  description: 'AppyWay Platform API — EntitlementTypes. 1 operations. Lead operation: AppyWay Fetch All Entitlement Types.
    Self-contained Naftiko capability covering one Appyway business surface.'
  tags:
  - Appyway
  - EntitlementTypes
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    APPYWAY_API_KEY: APPYWAY_API_KEY
capability:
  consumes:
  - type: http
    namespace: platform-entitlementtypes
    baseUri: https://api.appyway.com/v1/reference
    description: AppyWay Platform API — EntitlementTypes business capability. Self-contained, no shared references.
    resources:
    - name: fetchAllEntitlementTypes
      path: /fetchAllEntitlementTypes
      operations:
      - name: postfetchallentitlementtypes
        method: POST
        description: AppyWay Fetch All Entitlement Types
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.APPYWAY_API_KEY}}'
  exposes:
  - type: rest
    namespace: platform-entitlementtypes-rest
    port: 8080
    description: REST adapter for AppyWay Platform API — EntitlementTypes. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/fetchallentitlementtypes
      name: fetchallentitlementtypes
      description: REST surface for fetchAllEntitlementTypes.
      operations:
      - method: POST
        name: postfetchallentitlementtypes
        description: AppyWay Fetch All Entitlement Types
        call: platform-entitlementtypes.postfetchallentitlementtypes
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: platform-entitlementtypes-mcp
    port: 9090
    transport: http
    description: MCP adapter for AppyWay Platform API — EntitlementTypes. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: appyway-fetch-all-entitlement-types
      description: AppyWay Fetch All Entitlement Types
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: platform-entitlementtypes.postfetchallentitlementtypes
      outputParameters:
      - type: object
        mapping: $.
