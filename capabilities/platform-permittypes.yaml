naftiko: 1.0.0-alpha2
info:
  label: AppyWay Platform API — PermitTypes
  description: 'AppyWay Platform API — PermitTypes. 1 operations. Lead operation: AppyWay Fetch All Permit Types. Self-contained
    Naftiko capability covering one Appyway business surface.'
  tags:
  - Appyway
  - PermitTypes
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    APPYWAY_API_KEY: APPYWAY_API_KEY
capability:
  consumes:
  - type: http
    namespace: platform-permittypes
    baseUri: https://api.appyway.com/v1/reference
    description: AppyWay Platform API — PermitTypes business capability. Self-contained, no shared references.
    resources:
    - name: fetchAllPermitTypes
      path: /fetchAllPermitTypes
      operations:
      - name: postfetchallpermittypes
        method: POST
        description: AppyWay Fetch All Permit Types
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.APPYWAY_API_KEY}}'
  exposes:
  - type: rest
    namespace: platform-permittypes-rest
    port: 8080
    description: REST adapter for AppyWay Platform API — PermitTypes. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/fetchallpermittypes
      name: fetchallpermittypes
      description: REST surface for fetchAllPermitTypes.
      operations:
      - method: POST
        name: postfetchallpermittypes
        description: AppyWay Fetch All Permit Types
        call: platform-permittypes.postfetchallpermittypes
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: platform-permittypes-mcp
    port: 9090
    transport: http
    description: MCP adapter for AppyWay Platform API — PermitTypes. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: appyway-fetch-all-permit-types
      description: AppyWay Fetch All Permit Types
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: platform-permittypes.postfetchallpermittypes
      outputParameters:
      - type: object
        mapping: $.
